import { MystuDirective } from './mystu.directive';

describe('MystuDirective', () => {
  it('should create an instance', () => {
    const directive = new MystuDirective();
    expect(directive).toBeTruthy();
  });
});
